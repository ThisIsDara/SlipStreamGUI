# رابط گرافیکی اسلیپ استریم

**زبان‌ها:** [English](README.md) | [فارسی](README_FARSI.md)

---

## ⭐ معرفی کوتاه

‏SlipStream GUI یک رابط گرافیکی برای مدیریت تونل‌های DNS-over-HTTPS است. این ابزار با پایتون توسعه داده شده و امکان راه‌اندازی، کنترل و مانیتور هم‌زمان چندین session را فراهم می‌کند، همراه با لاگ زنده و مدیریت پراکسی ویندوز. 

---

## 📥 نصب و استفاده

### گزینه ۱: دانلود نسخه آماده (پیشنهادی)

1. برو به صفحه ریلیزها: https://github.com/ThisIsDara/SlipStreamGUI/releases
2. نسخه **SlipStream GUI v1.0.2** رو دانلود کن
3. فایل رو از حالت فشرده خارج کن
4. `SlipstreamGUI.exe` رو اجرا کن

> نیاز به نصب پایتون نداری!

### گزینه ۲: اجرا از سورس

**پیش‌نیازها:**
- Python 3.10 به بالا
- ویندوز

**مراحل:**

1. اول ریپو رو کلون کن یا فقط فایل `slipstream_gui.py` رو دانلود کن و توی یک پوشه قرار بده.

**کلون ریپو:**
```bash
git clone https://github.com/ThisIsDara/SlipStreamGUI.git
cd SlipStreamGUI
```

2. بعدش فایل باینری کلاینت رو از این لینک بگیر:
   - https://github.com/ThisIsDara/SlipStreamGUI/releases/download/SlipStreamGUI1.0.1/slipstream-binary.rar


3. فایل دانلود شده رو از حالت فشرده خارج کن.


4. فایل `slipstream-client-windows-amd64.exe` رو کنار `slipstream_gui.py` بگذار.

5. اگر خواستی آیکون هم درست نمایش داده بشه، فایل `stream.ico` رو هم تو همون پوشه بذار.

6. حالا برنامه رو اجرا کن:

```bash
python slipstream_gui.py
```

> **نکته:** Tkinter به‌صورت پیش‌فرض روی پایتون ویندوز هست، چیزی لازم نیست نصب کنی.

---

## 🚀 قابلیت‌ها

### 🔗 مدیریت اتصال‌ها
- **چند سشن همزمان**: چندتا کانکشن همزمان با تنظیمات مختلف
- **مانیتورینگ زنده**: نمایش PID، زمان اجرا، وضعیت سشن‌ها
- **دکمه‌های سریع**: کانکت، دیسکانکت، ریستارت
- **ذخیره تنظیمات**: ایمپورت و اکسپورت تنظیمات به JSON

### ⚙️ تنظیمات اتصال
- **تنظیم Resolver**: مشخص کردن هاست و پورت DNS
- **دامنه تونل**: تعیین دامنه هدف
- **مدیریت پورت TCP**: تنظیم پورت شنونده SOCKS
- **گواهی TLS**: در صورت نیاز اضافه کن
- **کنترل ازدحام**: انتخاب بین DCUBIC و BBR
- **گزینه‌های پیشرفته**: Keep-alive، Authoritative، UDP GSO

### 📋 مدیریت DNS
- **ایمپورت لیست DNS** از فایل متنی
- **انتخاب سریع** با دوبار کلیک
- **تشخیص خودکار** IPv4/IPv6 و پورت‌ها

## 🖼️ اسکرین‌شات

<img width="936" height="866" alt="screenshot4" src="https://github.com/user-attachments/assets/d7407e91-dc0e-4e9f-bbf9-8026523886cf" />

---

### 🖥️ یکپارچگی با سیستم
- **کنترل پراکسی ویندوز** با یک کلیک
- **آپدیت خودکار تنظیمات پراکسی**
- **رجیستری ویندوز** برای تنظیمات اینترنت
- **مدیریت درست پروسس‌ها**

### 📊 لاگ و مانیتورینگ
- **لاگ زنده** با رنگ‌بندی خروجی‌ها
- **نمایش جزئیات سشن‌ها**
- **پاکسازی خودکار سشن‌های بسته‌شده**
- **جلوگیری از اجرای چندباره برنامه**

### ✨ تجربه کاربری
- **تم دارک و مدرن**
- **اعتبارسنجی ورودی‌ها**
- **آپدیت بدون لگ**
- **راهنما و مقدار پیش‌فرض** برای فیلدها

### ⚡ کارایی و پایداری
- **رندر سریع ترمینال**
- **آپدیت غیرمسدودکننده** UI
- **مدیریت حافظه با محدودیت لاگ**
- **جلوگیری از کرش با هندل خطاها**

---

## 🙏 Credit

این GUI با استفاده از **Claude Opus 4.5** ساخته شده.
